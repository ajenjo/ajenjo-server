.container

  .h1.text-center: .logo-ajenjo.withLogo ajenjo

  //- h1.text-center= __("Cerrar Sesión")

  .row
    .col-md-4.col-md-offset-4: .well.well-xs

      div

        h3.text-center= __("Sesión Actual")
        h4.text-center {{$root.status.user.name || $root.status.user.user}}
        h4.text-center ({{$root.status.user.email}})

        br

        button(type="button", ng-click="close_session()").btn.btn-primary.btn-block= __("Cerrar Sesión")
