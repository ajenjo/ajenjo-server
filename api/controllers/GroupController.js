/**
 * GroupController
 *
 * @description :: Server-side logic for managing groups
 * @help        :: See http://links.sailsjs.org/docs/controllers
 */

module.exports = {
	des: function (req, res, next) {
    // // Group.findOne(req.param)
    // // next();
    // // return res.send('Hi! des.');

    // // console.log(res.socket);

    // var io     = sails.io;
    // var socket = req.socket;

    // // socket.join('room1');

    // // io.sockets.in('room1').emit('message_test',{
    // //   text: 'texto de prueba',
    // // });

    // console.log("El socket detectado es",socket);

    // return res.send(200);
  },
};

